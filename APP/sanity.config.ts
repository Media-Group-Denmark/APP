/**
 * This configuration is used to for the Sanity Studio that’s mounted on the `/app/studio/[[...index]]/page.tsx` route
 */

/* import {visionTool} from '@sanity/vision'
import {defineConfig} from 'sanity'
import {structureTool} from 'sanity/structure'
import { schemaTypes } from '../cms/schemaTypes'
import { imageType } from '../cms/schemaTypes/image/imageType'
import { media } from 'sanity-plugin-media'
import { scheduledPublishing } from '@sanity/scheduled-publishing'
import { assist } from '@sanity/assist'
import { daDKLocale } from '@sanity/locale-da-dk'
import '../cms/stylesheets/custom.css'
import { theme } from 'https://themer.sanity.build/api/hues?preset=tw-cyan'

export default defineConfig({
  basePath: '/studio',
  theme,
  projectId: 'ikbzpczn',
  dataset: 'production',
  
  // Add and edit the content schema in the './sanity/schema' folder
  schema: {
    types: [...schemaTypes, imageType],
  },
  plugins: [structureTool(),visionTool({defaultApiVersion: '2024-01-01'}), media(), scheduledPublishing(), daDKLocale()
  ],
}) */
